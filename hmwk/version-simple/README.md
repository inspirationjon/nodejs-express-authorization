POST - /login -> {"username": "Ilhom", "password": "hkm"} (JSON format)

GET - /posts <- put token to headers and get posts

POST - /posts -> {"title": "Vue 3 realeased!", "body": "Bla bla...."} (JSON format)
